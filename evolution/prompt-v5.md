# Prompt V5 - Bound Static Instance Facts

Use the demonstrated Ramen conversion gap to add only the collection-shape and
literal pure-conversion facts needed for bounded count/for_each and local-module
expansion. Keep symbolic expressions symbolic, preserve source ranges and safe
JSON, and do not grow tfconfig into a Terraform/OpenTofu evaluator or runtime.
